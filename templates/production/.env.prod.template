# Production Environment Configuration
#
# This file contains NON-SENSITIVE configuration
# Secrets are managed via Docker Secrets (see docker-compose.prod.yml)
#
# Usage:
#   cp .env.prod.template .env.prod
#   Edit values for your production environment
#   Deploy: docker compose -f docker-compose.prod.yml --env-file=.env.prod up -d

# Docker Registry
DOCKER_REGISTRY=registry.example.com
VERSION=1.0.0

# Database Configuration (host, port, name, user)
# Password is in Docker secret: /run/secrets/db_password
DB_HOST=db.example.com
DB_PORT=5432
DB_NAME=app_production
DB_USER=app_user

# Application Configuration
NODE_ENV=production
LOG_LEVEL=info
PORT=3000

# Non-sensitive API endpoints
API_BASE_URL=https://api.example.com
WEBHOOK_URL=https://app.example.com/webhooks

# Feature flags
ENABLE_ANALYTICS=true
ENABLE_CACHING=true

# Monitoring
SENTRY_DSN=https://public@sentry.io/project
APM_SERVICE_NAME=app-production

# Note: All sensitive values (passwords, API keys, tokens)
# are in Docker secrets, NOT in this file!
